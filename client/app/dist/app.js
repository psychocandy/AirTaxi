// Generated by CoffeeScript 1.3.3
(function() {
  var _ref;

  if ((_ref = window.AirTaxi) == null) {
    window.AirTaxi = {};
  }

  window.AirTaxi.DEV_MODE = true;

  $(document).ready(function() {
    var evt;
    document.addEventListener("deviceready", function() {
      var ApplicationRouter;
      window.AirTaxi.ErrorHandler = function(err) {
        return alert("[ERROR] code: " + err.code + " message: " + err.message);
      };
      ApplicationRouter = Backbone.Router.extend({
        routes: {
          "": "get_ride"
        },
        get_ride: function() {
          var getRideView;
          getRideView = new window.AirTaxi.Views.GetRideView();
          return window.t = getRideView;
        }
      });
      window.AirTaxi.Router = new ApplicationRouter();
      return Backbone.history.start();
    });
    if (window.AirTaxi.DEV_MODE) {
      evt = document.createEvent('Event');
      evt.initEvent('deviceready', true, true);
      return document.dispatchEvent(evt);
    }
  });

}).call(this);
