// Generated by CoffeeScript 1.3.3
(function() {
  var _base, _ref, _ref1;

  if ((_ref = window.AirTaxi) == null) {
    window.AirTaxi = {};
  }

  if ((_ref1 = (_base = window.AirTaxi).ViewHelpers) == null) {
    _base.ViewHelpers = {};
  }

  window.AirTaxi.ViewHelpers.BindModel = function(template_id, model) {
    var template, template_source;
    template_source = $("#" + template_id).html();
    template = Handlebars.compile(template_source);
    return template(model.attributes);
  };

  window.AirTaxi.ViewHelpers.BindCollection = function(template_id, collection) {
    var models_attributes, template, template_source;
    template_source = $("#" + template_id).html();
    template = Handlebars.compile(template_source);
    models_attributes = collection.map(function(model) {
      return model.attributes;
    });
    return template({
      models: models_attributes
    });
  };

}).call(this);
